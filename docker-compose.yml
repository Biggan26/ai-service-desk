services:
  api:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: ai-service-desk

    ports:
      - "8000:8000"

    env_file:
      - .env

    restart: unless-stopped
